name: tugas_5_5
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  
  # Image picker untuk upload foto dari galeri
  image_picker: ^1.0.4
  
  # Untuk icons iOS style
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets section yang sudah diaktifkan dengan indentasi 2 spasi
  assets:
    - assets/images/